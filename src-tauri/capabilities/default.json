{
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-spawn",
    "shell:allow-stdin-write",
    "shell:allow-open",
    "shell:allow-execute",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "exec-sh",
          "cmd": "/bin/sh",
          "args": ["-c", { "validator": ".*" }]
        }
      ]
    }
  ]
}
